# Contact form uses FormSubmit.co (see content/site-content.ts → contact.form.deliverTo).
# After deploy, submit the form once and confirm the activation email FormSubmit sends to that inbox.
#
# Optional local-only settings (none required for the contact form):
